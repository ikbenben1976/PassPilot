# PassPilot — Project Instructions

## Environment
- The user runs Windows (PowerShell). Never use Linux/macOS-only commands.
  - Use `Remove-Item -Recurse -Force` instead of `rm -rf`
  - Use `$env:VAR` instead of `export VAR=`
  - Use backslashes for paths in shell commands, or quote forward-slash paths
